/// [OperationState] enum contains all the operation states used in the app
/// it contains idle, waiting, done, success, failed, cancelled, error
/// It shows the operation state in the app for api calls
enum OperationState {
  idle,
  waiting,
  done,
  success,
  failed,
  cancelled,
  error,
}
