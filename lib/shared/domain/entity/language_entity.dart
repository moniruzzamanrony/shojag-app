/// [LanguageEntity] is used to represent the language of the app
/// It contains the locale and name of the language
class LanguageEntity {
  final String locale;
  final String name;

  const LanguageEntity({required this.locale, required this.name});
}
